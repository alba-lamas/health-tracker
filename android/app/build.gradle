android {
    namespace "com.example.symptom_tracker"  // o el nombre que prefieras
    compileSdkVersion flutter.compileSdkVersion
    
    defaultConfig {
        applicationId "com.example.symptom_tracker"  // o el nombre que prefieras
        minSdkVersion flutter.minSdkVersion
        targetSdkVersion flutter.targetSdkVersion
        versionCode 1
        versionName "1.0"
    }
} 